version: 2

models:
  - name: transformed_contract_category
    description: "Transformed contract category data."
    columns:
      - name: contract_category_id
        tests:
          - unique
          - not_null
      - name: category_name
        description: "Name of the contract category."
      - name: category_type
        description: "Type of the contract category."
